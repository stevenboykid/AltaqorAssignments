package altaqor.assignments.assignment21;

public interface BankAccount {

	//method signature (list of Method without body )
		void deposit (double amount);
		void withdrawl (double amount);
		double getBalance();
		double calculateInterest();

}
